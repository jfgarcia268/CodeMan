# macOS
.DS_Store

# Claude Code: track shared agents + skills (project tooling), ignore machine-local config.
.claude/settings.local.json
.claude/launch.json

# Local-only private context (deployment specifics, data provenance) — auto-loaded
# by Claude Code, never committed. Keep private notes here, not in CLAUDE.md.
CLAUDE.local.md

# CodeMan local-dev data (real data lives outside the repo via CODEMAN_DATA)
codeman/structures/

# Desktop wrapper build artifacts / deps
codeman-desktop/node_modules/
codeman-desktop/dist/
